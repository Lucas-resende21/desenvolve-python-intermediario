# 🧭 Aventura no Labirinto

Um jogo de exploração em terminal desenvolvido em Python, focado em conceitos de **recursividade**, **modularização** e interfaces de linha de comando (CLI).

## 🚀 Como Executar

### 1. Criar e Ativar o Ambiente Virtual
```bash
# No Linux/macOS
python3 -m venv venv
source venv/bin/activate

# No Windows
python -m venv venv
.\venv\Scripts\activate